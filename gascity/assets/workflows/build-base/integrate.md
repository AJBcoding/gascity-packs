This is the `build-base` integration stage. Treat it as a virtual contract that
concrete build methodologies may override only by preserving one typed,
verified candidate before implementation summary and review.

Expand the selected stock integration formula after the active implementation
drain completes. Pass the build artifact root, canonical manifest/result paths,
and on-demand integration target. Do not treat source-worktree commits or prose
summaries as an integrated candidate.

This stage has shadow authority only. It must not publish, update the target
ref, open a PR, or claim shipped state.
